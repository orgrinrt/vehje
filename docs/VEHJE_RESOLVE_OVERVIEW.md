<!--
  AUTO-GENERATED — DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-07-19T21:29:21Z
  Source: mock/
  To regenerate: cargo mock
-->

# vehje-resolve. Design

The framework's Core-level resolve pass. Name resolution over the shared
Core binders (`Let`, `Lambda`, `Var`), generic over the family set, with
family-extension hooks for family-specific resolution. This is a
framework-owned Core pass, not a consumer pass: the shared IR is only real,
not nominal, when the passes are shared (per the crate-surface topic). The
design authority is `202607192320`. `#![no_std]`, no alloc.

## Scope

- The resolve walk over Core binders, producing a resolution from each
 `Var` to the binder that introduced it.
- A borrowed, non-allocating scope chain: each scope frame lives on the
 walk's own stack and chains to its parent by reference, so no owned scope
 table is allocated. This is the no-alloc shape the prior std/`HashMap`
 resolver is replaced by.
- Family-extension hooks: a family's own binding forms extend the walk
 without the Core pass knowing them, composing with the family set the way
 the typestate axes do.
- Resolve diagnostics (unresolved name, duplicate binding).

## Non-scope

- Surface-syntax resolution (use-trees, module paths, visibility). Those
 vary with a consumer's grammar and live in the consumer, not the shared
 Core pass.
- Type checking (`vehje-check`).
- Manifest and crate-graph parsing. That was the first-party language's
 concern; it moves to that consumer.

## Dependencies

- `vehje-ir` for the Core nodes and binders.
- `hilavitkutin-str` for interned name identity.
- `hilavitkutin-api` for the family-set typestate the extension hooks
 compose over.
- `notko` for `Maybe` / `Outcome`.
- `#![no_std]`, no alloc.

## Deferred (BACKLOG)

The concrete family-extension registration surface (lands with the first
consumer family). Incremental re-resolution.
