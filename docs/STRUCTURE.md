<!--
  AUTO-GENERATED — DO NOT EDIT DIRECTLY

  Generated by: mockspace (mock)
  Generated at: 2026-07-24T06:45:01Z
  Source: mock/
  To regenerate: cargo mock
-->

# vehje — Structure Reference

> Auto-generated from the mock workspace. This document is the canonical
> description of every crate, type, and macro in the framework.

See also: [STRUCTURE.GRAPH.svg](STRUCTURE.GRAPH.svg) for the visual dependency graph.

## Table of Contents

**Layer 0 — IR**

- [fixpoint](#fixpoint)
- [ir](#ir)

**Layer 1 — Lex**

- [lower](#lower)
- [resolve](#resolve)
- [runtime-abi](#runtime-abi)
- [schedule](#schedule)
- [signature](#signature)

**Layer 2 — Syntax**

- [runtime-driver](#runtime-driver)
- [runtime-gen](#runtime-gen)
- [typecheck](#typecheck)

**Layer 3 — Resolve**

- [codegen](#codegen)

---

## Layer 0 — IR

### fixpoint

`vehje-fixpoint` · depth 0

**Depended on by:** lower, typecheck

---

### ir

`vehje-ir` · depth 0

**Depended on by:** codegen, lower, resolve, runtime-abi, runtime-driver, runtime-gen, schedule, signature, typecheck

---

## Layer 1 — Lex

### lower

`vehje-lower` · depth 1

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | ✓ (edge drawn) |
| fixpoint | ✓ | ✓ (edge drawn) |

#### Public API

- `Rewrite<'a>`
- `RuleTable`
- `ConstFold`
- `Cse`
- `Anf`
- `MacroExpand`
- `Lower`
- `LowerStrategy`

---

### resolve

`vehje-resolve` · depth 1

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | ✓ (edge drawn) |

**Depended on by:** typecheck

#### Public API

- `FamilyResolve`
  - `fn resolve_raw(_family: FamilyId, _payload: NodeList)`
- `CoreFamilies`
- `Scope<'p>`
- `Resolution<'a>`
- `ResolveError`
- `fn resolve(arena: &Arena<'_>, root: NodeRef) → Outcome<(), ResolveError>`
- `fn resolve_with<H: FamilyResolve>(arena: &Arena<'_>, root: NodeRef, hook: &H) → Outcome<(), ResolveError>`
- `fn resolve_into(arena: &Arena<'_>, root: NodeRef, resolution: &mut Resolution<'_>) → Outcome<(), ResolveError>`
- `fn resolve_into_with<H: FamilyResolve>(arena: &Arena<'_>, root: NodeRef, resolution: &mut Resolution<'_>, hook: &H) → Outcome<(), ResolveError>`

---

### runtime-abi

`vehje-runtime-abi` · depth 1

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | ✓ (edge drawn) |

**Depended on by:** runtime-driver

---

### schedule

`vehje-schedule` · depth 1

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | ✓ (edge drawn) |

**Depended on by:** runtime-driver

#### Public API

- `Pass`: : core::fmt::Debug
- `PassDag`
- `Schedule`
- `ScheduleError`

---

### signature

`vehje-signature` · depth 1

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | ✓ (edge drawn) |

**Depended on by:** runtime-gen, typecheck

#### Public API

- `Operation`
- `LeaseRule`
- `TargetDecl<'a>`
- `Signature<'a>`
- `Projection`
- `GrammarHook`

---

## Layer 2 — Syntax

### runtime-driver

`vehje-runtime-driver` · depth 2

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | — (transitively implied) |
| runtime-abi | ✓ | ✓ (edge drawn) |
| schedule | ✓ | ✓ (edge drawn) |

#### Public API

- `Reader<'a>`
- `DriverError`
- `fn dispatch(_residual: &Residual<'_>) → Outcome<(), DriverError>`

---

### runtime-gen

`vehje-runtime-gen` · depth 2

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | — (transitively implied) |
| signature | ✓ | ✓ (edge drawn) |

#### Public API

- `Slice<'a>`
- `Package<'a>`
- `Manifest`
- `DifferentialCheck`
- `SliceKind`
- `fn generate<'a>(_signature: &Signature<'a>, slices: &'a [Slice<'a>]) → (Package<'a>, Manifest)`

---

### typecheck

`vehje-typecheck` · depth 2

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | — (transitively implied) |
| resolve | ✓ | ✓ (edge drawn) |
| signature | ✓ | ✓ (edge drawn) |
| fixpoint | ✓ | ✓ (edge drawn) |

**Depended on by:** codegen

#### Public API

- `FamilyCheck`
  - `fn effect_of_raw(_family: FamilyId) → EffectMask`
- `Checked<'a, T>`
- `Graded<'a>`
- `CheckError`
- `fn mint_checked<'a, T, Families, Effects>(graded: Graded<'a>, _inclusion: PhantomData<(Families, Effects)>) → Checked<'a, T>`
- `fn check<'a>(arena: &'a Arena<'a>, root: NodeRef, res: &Resolution<'_>, grades: &mut GradeTable<'_>) → Outcome<Graded<'a>, CheckError>`
- `fn check_with<'a, H: FamilyCheck>(arena: &'a Arena<'a>, root: NodeRef, res: &Resolution<'_>, grades: &mut GradeTable<'_>, hook: &H) → Outcome<Graded<'a>, CheckError>`

---

## Layer 3 — Resolve

### codegen

`vehje-codegen` · depth 3

#### Dependencies

| Dependency | Direct | Transitive-reduced |
|------------|--------|--------------------|
| ir | ✓ | — (transitively implied) |
| typecheck | ✓ | ✓ (edge drawn) |

#### Public API

- `Target`: : TargetSets + core::fmt::Debug
  - `fn emit<S: ByteEmitter>(checked: &Checked<'_, Self>, sink: &mut S) → Outcome<(), CodegenError>`
- `CodegenError`
- `fn check_for<'a, T, Families, Effects>(graded: Graded<'a>) → Checked<'a, T>`
- `fn fold_core<F: FnMut(&Node)>(arena: &Arena<'_>, at: NodeRef, visit: &mut F)`

---

