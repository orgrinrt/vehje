// generated by mockspace-bench-matrix; never hand-edited.
use mockspace_bench_core::FfiBenchCall;
use mockspace_bench_macro::bench_variant;

#[bench_variant("abi_payload_cost_tight_null_entry", sizes = [1, 4, 16, 64, 256, 1024])]
fn run<const N: usize>(input: &[u8; N], output: &mut [u8; 8]) -> FfiBenchCall {
    mockspace_bench_matrix::scaffold::warm::<N, _, _, _>(
        input,
        output,
        move |n| vehje_bench_carrier::bench::boundary::payload::setup_null_entry("tight", n),
        vehje_bench_carrier::bench::boundary::payload::cell_null_entry,
    ).into_ffi()
}
