// generated by mockspace-bench-matrix; never hand-edited.
use mockspace_bench_core::FfiBenchCall;
use mockspace_bench_macro::bench_variant;

#[bench_variant("carrier_coldcycle_scatter_fntable", sizes = [64, 256, 1024])]
fn run<const N: usize>(input: &[u8; N], output: &mut [u8; 8]) -> FfiBenchCall {
    mockspace_bench_matrix::scaffold::cold_cycle::<N, _, _, _>(
        input,
        output,
        move |n| vehje_bench_carrier::bench::coldcycle::setup("scatter", n),
        vehje_bench_carrier::bench::coldcycle::cell_fntable,
    ).into_ffi()
}
