[workspace]
[package]
name = "carrier_dispatch_madd_threaded"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
name = "carrier_dispatch_madd_threaded"
path = "src/lib.rs"
crate-type = ["cdylib"]
[dependencies]
mockspace-bench-core = { path = "../../../../../mockspace/bench-core", features = ["std"] }
mockspace-bench-macro = { path = "../../../../../mockspace/bench-macro" }
mockspace-bench-matrix = { path = "../../../../../mockspace/bench-matrix", default-features = false }
vehje-bench-carrier = { path = "../../carrier", features = ["threaded"] }
[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
