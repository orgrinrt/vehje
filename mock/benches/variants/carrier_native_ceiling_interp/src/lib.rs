// generated by mockspace-bench-matrix; never hand-edited.
use mockspace_bench_core::FfiBenchCall;
use mockspace_bench_macro::bench_variant;

#[bench_variant("carrier_native_ceiling_interp", sizes = [64, 256, 1024])]
fn run<const N: usize>(input: &[u8; N], output: &mut [u8; 8]) -> FfiBenchCall {
    mockspace_bench_matrix::scaffold::stream::<N, _, _, _>(
        input,
        output,
        move |n| vehje_bench_carrier::bench::native_ceiling::setup("", n),
        vehje_bench_carrier::bench::native_ceiling::cell_interp,
    ).into_ffi()
}
