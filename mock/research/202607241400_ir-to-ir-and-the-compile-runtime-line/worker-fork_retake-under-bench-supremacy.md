# Full re-take under bench supremacy: hard data rules over topics rules over changelists

**Date:** 2026-07-24
**Author:** worker fork, re-take directive
**Precedence rule (op, 2026-07-24):** benches and their findings (hard data, measurements) trump topics; topics trump changelists. This re-take applies that order to every bench-backed claim in the round, and it supersedes the bench-related conclusions of my two prior deliverables in this dir (`worker-fork_canonical-drift-audit-and-panel-mirror.md`, `worker-fork_changelist-drift-and-claim-verification.md`) and of the panel (`tiark_rompf...`, `chris_fallin...`) wherever they conflict with the hard data. Non-bench findings from those files are carried forward unchanged and summarized at the end.
**Bench authority established below:** `mock/benches/results/RUN_SUMMARY.md`, the 58-bench carrier composition matrix re-run with the PMU pass armed, audited by a four-expert panel (`mock/research/202607230922_carrier-matrix-result-review-panel/`), which by its own statement "supersede[s] the first-run summary" because "several first-run headlines were artifacts of the reporting layer."

## Why the precedence rule changes the audit

My first two deliverables adjudicated the native-ceiling dispute by topic order (the later topic `240015` re-stated 1.2x, and topics trump research docs). That reasoning is now obsolete: under the corrected rule the question is not "which topic is later" but "what does the most rigorous measurement show, and did the topics carry it faithfully." The carrier PMU re-run is that measurement: it is the latest, it is PMU-instrumented (instructions/cycles/IPC per variant), it was audited by a four-expert panel that caught and corrected first-run artifacts, and it explicitly supersedes both the earlier carrier first-run and the standalone benches the topics quote. So it is the bench authority, and where a topic's locked number disagrees with it, the topic drifted from the data.

Applying this exposes a THIRD layer of drift the first two deliverables did not have precedence to see. The drift is now three-deep: the benches are the ground truth; the topics drifted from the benches on three specific numbers; the changelists drifted from the topics on two mandates (CFG, signature); and the subsequent rounds built on both. op's felt "incomplete, spirit-drifted" state is the accumulation of all three.

## The corrected bench-claim ledger

Each row: what a topic locked as "settled," what the authoritative hard data shows, the verdict under bench supremacy.

### VINDICATED by the hard data (topic and bench agree; a panelist was wrong to doubt)

- **Native throughput ceiling.** Topic locked "the native 1.0x-to-1.5x ceiling" (`202607240100:419`). Hard data: "Native throughput ceiling is ~1.5x over the best interpreter" (`RUN_SUMMARY.md:108`), `carrier_native_ceiling` native 1.49x over the predecoded interpreter at N=1024 (`:440-446`), with the larger 2.6-4.3x wins explicitly only over the plain (non-best) interpreter. **VINDICATED.** Fallin's Finding F (`chris_fallin...:227-256`, "the canon cites retracted evidence; real is ~2.0x") does NOT hold: his 2.0x came from the `202607220300` remeasure, which the later PMU carrier re-run superseded at ~1.5x. He cited a doubly-stale intermediate. My own first-deliverable framing (an "unreconciled 1.2x-vs-2.0x conflict") is also superseded: the conflict IS reconciled, by the authoritative run, at ~1.5x over the best interpreter. Canon correct.
- **Vertical SIMD is the biggest throughput lever, justifying the batched-column ABI.** Topic: the batched-column `extern "C"` entry is "justified by the vertical-SIMD vectorisation win (the biggest measured throughput lever)" (`202607240100:132`). Hard data: "Vertical SIMD is the single biggest lever: vert8 up to 4.8x" (`RUN_SUMMARY.md:100`), magnitude corrected down from a first-run 6x but still the top lever and "the one ABI-shaping result." **VINDICATED** (magnitude 4.8x not 6x).
- **Register interpreter beats stack; stack-bytecode middle tier closed.** Consistent with the CFG-of-blocks register interpreter the topic locked. Register wins 1.41-3.06x on the corrected symmetric baseline (`RUN_SUMMARY.md` finding 4). **VINDICATED.**

### REVISED by the hard data (topic locked a number or framing the authoritative bench corrects)

- **Dispatch: switch-beats-threading is 10-35%, not 1.7x, and threading WINS on real control flow.** Topic locked "plain-switch dispatch [beats tail-threading]" citing ~1.7x (`202607240015:58-66`, `202607240100:419`). Hard data: "Dispatch shape matters 10 to 35% at the honest size; the small-N ties were a predictor-memorization artifact" (`RUN_SUMMARY.md` finding 1), and decisively, "Threading wins only where control flow is real... on the CFG workload (`carrier_cfg`), `trace` runs at 0.44x of `switch`... 2.25x fewer [instructions]... Preserve-none threading is a control-flow lever, not a general dispatch win" (finding 2). **REVISED, and this is a live drift-from-bench:** the topic's flat "switch beats threading 1.7x" is wrong on magnitude (10-35% on straight-line) AND on framing (threading is not just-worse; it is the correct dispatch for real-control-flow residuals). This interacts with the CFG-of-blocks decision below: the runtime's own execution model IS a CFG with terminators and back-edges, exactly the "real control flow" regime where the hard data says threading wins. The topic dismissed the dispatch strategy its own runtime model favors.
- **The dev-time equality-saturation e-graph is contradicted as the valuable heavy stratum.** Topic enshrines it: "The dev-time e-graph is the one genuinely original research piece" (`202607240100:101`), deferred as "the second stratum inside `vehje-lower`" (`:387`). Hard data: "ship fold+CSE+DCE always on; eqsat is OUT... eqsat's marginal contribution to [the pipeline] is zero-or-negative... the first run's 142x was eqsat-alone pessimization presented as a win... Ship fold+CSE+DCE; park eqsat/DAG-aware extraction behind a named trigger" (`RUN_SUMMARY.md` finding 6). **REVISED, and this is the largest drift-from-bench in the round:** the topic treats the e-graph as the crown-jewel original research worth deferring the whole round's frontier budget for, while the round's own later, panel-audited data says the cheap fold+CSE+DCE subset IS the optimizer and eqsat contributes zero-or-negative and belongs behind a named trigger, not as the intended heavy stratum. Both panelists inherited the topic's over-valuation: Rompf's finding 2 (`tiark_rompf...:28-31`, "the cheap fixpoint and the e-graph are the same operation at two precisions") and Fallin's open-question 1 (`chris_fallin...:276-278`, "wire `vehje-fixpoint` into `LowerStrategy::Saturate` as the first real client") both spend design attention on a stratum the hard data has parked. Under bench supremacy, the e-graph is a park-behind-a-trigger maybe, not the frontier centerpiece.
- **Native tier: direct instruction-selection beats copy-and-patch for short-lived residuals; the deferred stencil crate is not the best native form.** Topic: runtime generation emits "a copy-and-patch stencil" per operation (`202607240100:113`), and the deferred `vehje-stencil` is the copy-and-patch toolchain (`:348`). Hard data: direct isel and copy-and-patch tie at warm speed, but "Direct isel compiles ~2x cheaper than copy-and-patch on every profile... for short-lived residuals, direct isel is the better native tier (equal warm speed, half the compile cost), and predecode dominates when the residual runs only a few times" (`RUN_SUMMARY.md` findings 7-8). **REVISED:** the native-tier plan is copy-and-patch-centric where the data says direct isel wins for the short-lived residuals the authoring/templating majority produces, and predecode wins for few-run. The stencil toolchain is not the priority the taxonomy implies.
- **Record/operand width is a null result for throughput.** Topic: operands stay inline, "only the exact inline count is unlocked" as bench-gated (`202607240015:55-56`). Hard data: "Record width is a null result: a density choice, refuting 24-byte-optimal... within 1% at every size... Choose REC16 for density" (`RUN_SUMMARY.md` finding 3). **REFINED:** the open "inline count" is not a speed question at all; pick 16 bytes for cache density. Consistent with "count open," but the bench closes it as a density knob, not a throughput lever.

### The eqsat correction, stated carefully

The bench does not say equality saturation is worthless in all futures; it says its marginal contribution over fold+CSE+DCE is zero-or-negative on the measured corpus and it should sit behind a named trigger. The drift is the topic's framing that the e-graph is "the one genuinely original research piece" worth orienting the frontier around. Under bench supremacy that framing is not supported; the honest statement is "fold+CSE+DCE is the optimizer, benched; DAG-aware extraction / eqsat is a conditional accelerator behind a trigger, of unproven marginal value." This matters for the live redirection: the IR-to-IR reframing builds exactly the fold+CSE+DCE cheap subset the hard data endorses, so the redirection is aimed correctly; the e-graph discussions in both panel files are lower priority than the topic (and the panelists) assumed.

## The three-layer drift, assembled

1. **Benches (ground truth) to topics.** The consolidation `202607240100` (07-24) folded in the carrier native ceiling (~1.5x, correct) but did NOT fold in three other findings from the same 07-23 panel-audited carrier run: the dispatch revision (threading wins on CFG), the eqsat-is-out correction, and the direct-isel-over-copy-and-patch reorder. It locked older standalone-bench framings for these ("switch 1.7x", "e-graph is the original research piece", "copy-and-patch stencil") as "settled and not reopened." So the topic is internally inconsistent with its own round's latest data on three counts. This is the drift op's new rule is built to catch, and it is real.
2. **Topics to changelists.** Independent of the bench layer, the changelists reduced two topic MANDATES to weaker surface checks (verified in the prior deliverable): the CFG-of-blocks interpreter (`202607240100:117-124`) became "the struct has block-table fields" (`202607240200:133-137`), satisfiable by dead type surface; and "check reads the `vehje-signature` schema" (`:296`) became an unverified prose claim the impl never honored (no `use vehje_signature` in `vehje-typecheck/src/`).
3. **Changelists to subsequent rounds.** The CFG surface-check let the deepdive round assert a CFG interpreter as shipping contract in `vehje-runtime-abi`'s DESIGN while only a flat tree-re-tag encoder exists; the unverified signature claim let the graded-check rounds grow a check pass that bypasses the single-source signature. Both deepened silently because no marker flagged either gap.

The felt drift is the sum: a runtime described as a CFG-of-blocks interpreter that is a tree-walker, a single-source signature nothing reads, a frontier oriented toward an e-graph the data parked, a dispatch decision that dismisses the strategy the runtime's own control-flow model favors, and a native tier built around the costlier of two tied codegen methods. None is a wrong direction; all are the design and the docs running ahead of, or contrary to, the hard data and the wiring.

## What this means for the live IR-to-IR redirection

- **Endorsed by the data:** building the fold+CSE+DCE cheap subset as the IR-to-IR emit path is exactly what finding 6 says is the optimizer. Proceed. Rompf's hash-consing-Builder collapse of CSE (finding 5 in his file) is consistent with "fold+CSE+DCE always on, cheapest form."
- **Redirected by the data:** do not orient the redirection around wiring the e-graph (`LowerStrategy::Saturate`); the data parks it behind a trigger. Keep the `Saturate` seam as a reserved conditional accelerator, not the frontier goal.
- **A real coupling to settle:** the CFG-of-blocks question (does `Tier::Arena` need a real CFG) now couples to the dispatch finding. If the runtime executes a CFG-of-blocks residual, the hard data says threading (a control-flow lever) wins on that workload, so the "plain switch, threading rejected" decision must be revisited for the terminator/back-edge path specifically. Settle the CFG shape and the dispatch strategy together, from the bench, not from the topic's dismissal of threading.
- **The native tier for the majority:** direct isel, not copy-and-patch, for the short-lived residuals authoring/templating produces; predecode for few-run. Reweight the deferred `vehje-stencil` accordingly.

## Meta-review of the prior deliverables under the corrected precedence

- **My own two prior files:** the native-ceiling adjudication (I concluded "canon not citing retracted evidence" via topic order) reaches the right verdict but by the now-obsolete reason; the correct reason is the authoritative carrier bench vindicates 1.0-1.5x directly. My "unreconciled bench conflict" caveat is withdrawn: the PMU re-run reconciles it at ~1.5x. Everything non-bench in those files stands (CFG gap, signature-read gap for the check half, binding-time inertness, the two changelist defects, the cascade). The runtime-gen-half overstatement correction I already made stands.
- **Fallin:** Finding F is wrong under bench supremacy (doubly-stale 2.0x). His Findings A (NodeRef arena identity), B (`fold_core` is a visitor not a catamorphism), C (CFG unbuilt and unflagged), D (`binding_time_ceiling` on `Operation`), and E (node-count cap from the eqsat bench) all stand, and E is reinforced: the same bench corpus that supplies the cap also says eqsat is marginal, so the cap matters more for the cheap subset than for the parked e-graph. His instinct that a stale number is loose in the docs is correct in spirit; he picked the wrong number as the truth. Note also: his open-question 1 (wire the e-graph) is de-prioritized by finding 6.
- **Rompf:** his frame (binding-time lattice) is untouched by the bench layer and remains the canon's own axis, correct and load-bearing. His finding 2 (cheap fixpoint and e-graph are one operation) is theoretically sound but its practical weight drops: the e-graph half is parked by the data, so the unification is a smaller prize than stated. His macro-hygiene finding stands.

## Meta-review of the concurrent Fallin canon-recheck (`chris_fallin_canon-recheck-and-drift-audit.md`, appeared during this write)

A second Fallin deliverable landed in this dir while I wrote the re-take. Reviewed in full and mirrored against the canon and the bench-supremacy rule.

- **His Finding 1-2 (the CFG compounding-drift chain) is the strongest thing in either of our audits, and it holds.** He traced the CFG gap to the SKETCH: `sk17_cfg-blocks-interpreter/findings.md` proves an interpreter over an already-block-shaped program and its own text says "validates the execution model," NOT the lowering; the `240015` evidence topic then cites it as the whole mechanism "WORKS" (`240015:105-106`), overstating the sketch; the src CL ships type surface with a true-but-incomplete claim and no FIXME; and both newer notes (`241000`, `241300`) then re-certify past the gap, `241000` on the untested premise that "the serialize half is doable" and `241300` by the FIXME-population method that structurally cannot see an unmarked gap. This is the `canonical-design-outranks-intermediate-rounds` failure named precisely: three artifacts trusting each other instead of the topic and the source. Confirmed; it sharpens my own Part C cascade with the sketch as the origin.
- **His Finding 2 (the ANF to CFG dependency) is genuinely new and important, and it connects to the live redirection.** The CFG-of-blocks lowering is downstream of `Anf` specifically (a nested `If` has no well-defined successor block until ANF names its result at a fixed point; Flanagan et al. 1993, Appel "SSA is Functional Programming" 1998, block-arguments = the topic's "block-argument-passed loop-carried values"). And `Anf` is itself downstream of the IR-to-IR emit path (it creates nodes the redirect-only `Rewrite` cannot). So the true causal chain is: IR-to-IR emit path -> `Anf` -> CFG-of-blocks lowering -> the `Tier::Arena` residual the runtime interprets. Nothing in the taxonomy names this chain, so three coupled deferrals read as unrelated. This materially strengthens the case for the IR-to-IR emit path as the FIRST domino, not just a macro/const-fold convenience: it unblocks ANF, which unblocks the CFG the canon settled. Adopt his fix (cross-link the FIXMEs into one named chain).
- **His Finding 3 (unbounded fixpoint) is reinforced and I upgrade my own accordingly.** He found the round's own evidence topic states, in its own words, that a bounded streaming window is "a hard termination-and-memory safety mechanism" for exactly the growing-arena fixpoint class (`240015:192-195`). So bounding the IR-to-IR fixpoint is mandated by the round's own prior evidence, not merely good practice. Note the eqsat nuance this exposes, which cuts in my re-take's favor: `240015`'s eqsat statement validates two DIFFERENT things, tameness ("5.2x linear, not exponential", eqsat does not explode, so bounded-window is safe) and, separately, the consolidation frames eqsat as valuable ("the one genuinely original research piece"). The carrier bench finding 6 answers only the VALUE axis (zero-or-negative marginal, OUT), not the tameness axis. So there is no conflict between Fallin's Finding 3 (tameness -> bound is mandatory) and my re-take's eqsat finding (value -> park it): eqsat is both tame AND marginal, safe to run and not worth running. Both hold; I sharpen my re-take's eqsat row to name the value-vs-tameness distinction so it does not read as overclaiming eqsat is unsafe.
- **His Finding 4 (native ceiling) is a fair caution on my re-take's central claim, and we converge once the baseline is named.** He walks back his own earlier 2.0x and argues three non-reconciled numbers exist (1.0-1.2x original/suspect, ~2.0-2.2x from `220300`, ~1.5x from the composition matrix), none standing down for another, so picking any one (his 2.0x, or my ~1.5x) is premature. He is right that I asserted ~1.5x as settled without showing the reconciliation. The reconciliation is available in the carrier data itself: finding 10 states native is "~1.5x over the BEST interpreter" and "the larger 2.6 to 4.3x native wins are all measured against the plain warm interpreter, not its best form" (`RUN_SUMMARY.md:108-112`), so the spread is baseline-dependence, and `220300`'s ~2.0x was over a switch interpreter (a lesser baseline than the carrier's predecoded one). So the corrected joint position: the honest ceiling OVER THE BEST (predecoded) interpreter is ~1.5x, which is the meaningful number (you ship the best interpreter) and vindicates the canon's 1.0-1.5x; `220300`'s ~2.0x is not a contradiction but a different-baseline measurement; the one residual owed check is confirming `220300`'s interpreter baseline was the lesser form, which closes the gap rather than leaving it open. This is tighter than either his "owed reconciliation, cite neither" or my "carrier settles it flatly": the data reconciles once the baseline is stated, and the canon's number survives. Neither of us should cite a bare number without the "over the best interpreter" qualifier.
- **Full agreement:** CFG unbuilt and unflagged, both newer notes miss it, `241300`'s FIXME-method blind spot, the unbounded-fixpoint gap, `NodeRef` arena-brand and macro hygiene as the two open soundness items untouched by any topic or note, the honestly-flagged rename, and the twelve-crate/`Handle`/layering checks all holding. No disagreement on any of these.

Net: his canon-recheck and this re-take are complementary, not competing. He owns the CFG-origin-in-the-sketch chain and the ANF dependency (both new, both correct); I own the bench-supremacy re-adjudication of dispatch, eqsat-value, and native-form (all three topic-drifts-from-benches his recheck does not cover, because it re-read the two closing topics and the two notes but not the carrier PMU `RUN_SUMMARY`). Together the two files give the full three-layer drift picture with both the CFG causal chain and the bench-precedence corrections. His Finding 4 correctly tightened my native-ceiling claim; my carrier-data reading correctly closes the reconciliation he left open.

## Non-bench findings carried forward unchanged (from the prior two deliverables)

The signature is functionally inert (check never reads it in code, the mandated-now half); the binding-time axis is inert (`vehje-typecheck/src/lib.rs:311-312`); the CFG-of-blocks interpreter is locked canon, unbuilt, unflagged; the IR-to-IR topic reopens an "unbounded" fixpoint the canon locked shut (and the bench supplies the exact node-count cap to close it); macro hygiene is unaddressed in canon and impl; the two changelist defects (surface-check standing in for mandate-satisfaction) are the drift seeds; the `vehje-check` rename is honestly flagged and open; `241300`'s deferral-boundary audit is over-generous because it audited marked FIXMEs and the load-bearing gaps are unmarked. All hold.

## Corrected bottom line and the repairs, in priority order

The round's direction is sound and the identity is intact; the drift is three-layered and entirely one of the design and docs and wiring running ahead of, or against, the hard data. Repairs:

1. **Correct the topic's "settled bench-backed corrections" on three counts** (this is the bench-supremacy fix op's rule mandates): dispatch is 10-35% with threading winning on real control flow (not switch-1.7x-flat); fold+CSE+DCE is the optimizer and eqsat is a park-behind-a-trigger conditional (not the frontier centerpiece); direct isel beats copy-and-patch for short-lived residuals (not copy-and-patch-centric). File as a doc round against the consolidation's "settled" list and `DEEPDIVE_OUTPUT_SPECTRUM`.
2. **Wire the check pass to read the signature** (Fallin D as the forcing function) so the single-source soul stops being inert.
3. **Mark then resolve the CFG-of-blocks gap**, and settle the CFG shape together with the dispatch strategy from the bench (threading on the control-flow path), not from the topic's dismissal.
4. **Bound the IR-to-IR fixpoint** with the node-count cap the eqsat bench validates (Fallin E), and build the cheap subset as the endorsed optimizer while keeping `Saturate` a reserved conditional seam, not the goal.
5. **Wire the binding-time axis** (re-lands the graded soul and answers the panel's compile/runtime question from the canon's own axis).
6. **Reweight the native tier** toward direct isel and predecode per the bench; de-prioritize the copy-and-patch stencil.
7. **Process fix:** a topic that locks a bench-backed number must cite the latest, most-rigorous run and be re-derived when a later run supersedes it; the consolidation locked three numbers its own round's later carrier PMU re-run had already revised, and a "settled and not reopened" list is exactly where that staleness hides. Under op's precedence rule, "settled" means "settled by the latest measurement," not "settled by the topic that quoted an earlier one."
