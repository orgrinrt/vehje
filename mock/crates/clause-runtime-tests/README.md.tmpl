# vehje-runtime-tests. ABI soundness tests

size_of / align_of / offset_of assertions on every ABI type.
Minimal Zig stub dylib for round-tripping token / node /
diagnostic buffers. Proves the ABI handshake without requiring
the real runtime.
