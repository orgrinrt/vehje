# clause-runtime-driver — compiler-side runtime dispatcher

Dlopens the shipped Zig runtime dylib, validates abi_version,
dispatches macro / scratch / generative calls through the ABI.
Consumed by clause-schedule (macro expansion pass) and by
clause-codegen (generative expansion pass).
