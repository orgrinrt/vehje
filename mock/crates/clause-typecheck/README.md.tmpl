# vehje-typecheck — types + coherence + validator framework

Consumes a resolved program graph, produces a typed graph. Runs
the validator framework (R4 scope). Handles generics, bind-target
resolution, coherence checks, and type-level analyses.
