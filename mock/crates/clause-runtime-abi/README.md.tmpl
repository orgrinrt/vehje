# clause-runtime-abi — C ABI contract

Defines the C ABI for compiler ↔ runtime communication. Pure
`#[repr(C)]` types + `extern "C"` entry points. No runtime logic.
Consumed by clause-runtime-driver (Rust side) and
clause-runtime-zig (Zig side).
