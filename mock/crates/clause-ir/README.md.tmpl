# clause-ir — shared IR types

Leaf-dep crate carrying types shared across every compiler phase:
span, file id, node id, scope id, diagnostic, token kind discriminator,
AST node kind discriminator. No algorithms — just the data contract
every phase reads and writes.
