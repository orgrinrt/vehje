# vehje-schedule. pass DAG

The framework's compile-side pass scheduler: a DAG of Core passes over
`arvo-graph`, single-shot topological walks (not hilavitkutin morsel
execution, which is the runtime side). Orders resolve, check, and a
consumer's family passes by their declared dependencies. `#![no_std]`,
no alloc.
