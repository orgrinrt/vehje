# vehje-runtime-driver. compiler-side runtime dispatch

The compiler-side dispatch over the tier-tagged C ABI: serialize a
`Checked` residual at the chosen tier, hand it to the embedded Zig runtime,
map results and diagnostics back. Compile-time dispatch, no `dyn`, no
plugin shared-object loading at the compiler layer. `#![no_std]`, no alloc.
