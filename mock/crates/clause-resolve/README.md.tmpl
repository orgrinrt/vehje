# clause-resolve — module resolution + name binding

Consumes AST from clause-syntax, produces a resolved program graph
with bound names. Drives the workspace crate graph via
Clause.toml manifest parsing. Handles use-tree expansion, visibility
rules, and scope construction.
