# vehje-resolve — module resolution + name binding

Consumes AST from vehje-syntax, produces a resolved program graph
with bound names. Drives the workspace crate graph via
Vehje.toml manifest parsing. Handles use-tree expansion, visibility
rules, and scope construction.
