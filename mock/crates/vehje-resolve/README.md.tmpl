# vehje-resolve. Core resolve pass

The framework's Core-level resolve pass: name resolution over the shared
Core binders, generic over the family set, with family-extension hooks. A
borrowed non-allocating scope chain, not owned tables. Consumes vehje-ir
Core nodes; a consumer's family passes extend it for family-specific
resolution. `#![no_std]`, no alloc.
