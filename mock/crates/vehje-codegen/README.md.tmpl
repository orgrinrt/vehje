# vehje-codegen. target-agnostic codegen

Consumes a scheduled IR from vehje-schedule, emits the target-
agnostic representation. Extension targets (vehje-jomini,
vehje-w3, vehje-lua, ...) plug in via the extension-point
contract finalised in R3.
