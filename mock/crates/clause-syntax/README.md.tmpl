# clause-syntax — parser + AST

Consumes a token stream from clause-lex, produces a concrete syntax
tree. Hosts the G0 grammar framework primitives (cursor, stream,
node arena). Emits syntax diagnostics for unexpected tokens.
