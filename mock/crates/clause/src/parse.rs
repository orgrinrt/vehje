//! `clause parse` — stub. Real parser body lands in a follow-up round.

pub fn run(_args: &[String]) -> i32 { // lint:allow(bare_string) lint:allow(bare_numeric) reason: CLI subcommand entry; argv and exit code are std-boundary shapes; tracked: #73
    eprintln!("parse: not yet implemented");
    2
}
