---
round: 202605110400
phase: SRC
scope: vehje-codegen (one in-source doc-comment)
---

# Src CL: codegen ctx.rs doc-comment fix

One file, one doc-comment block. The struct, impl, and function
signatures are unchanged. Only the prose in the `///` doc-comment on
`CodegenCtx` flips to describe the post-Round-5d emit() / sinks
contract instead of the retired `CodegenArtifact.diagnostics` shape.

## Files

- `mock/crates/vehje-codegen/src/ctx.rs`

## CHANGE: rewrite stale CodegenCtx doc-comment

File: `mock/crates/vehje-codegen/src/ctx.rs:13-19`
Verification: `! grep -n "CodegenArtifact\.diagnostics" mock/crates/vehje-codegen/src/ctx.rs` returns no match. `grep -n "DiagnosticSink<Diagnostic>" mock/crates/vehje-codegen/src/ctx.rs` returns one match (in the doc-comment).

Before:
```
/// Holds the resolver output. Targets should not mutate anything
/// through the context, diagnostics come back via the
/// `CodegenArtifact.diagnostics` field on the return value. A
/// mutable `DiagnosticSink` + `ArtefactCache` pair is BACKLOG
/// for once emission volume justifies it.
```

After:
```
/// Holds the resolver output. Targets receive the context plus two
/// caller-owned sinks (`&mut dyn ByteEmitter` and
/// `&mut dyn DiagnosticSink<Diagnostic>`); diagnostics push into the
/// sink, bytes push into the byte emitter, and the returned
/// `CodegenArtifact` carries only an `ArtifactKind`. A future
/// `ArtefactCache` for incremental codegen is BACKLOG.
```

## Lock criteria

- `cargo check -p vehje-codegen` green.
- `cargo mock --lint-only --commit` green.
- No `*.md.tmpl` diff.
