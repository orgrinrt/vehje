**Date:** 2026-05-10
**Phase:** TOPIC
**Round:** 202605110400 (vehje-codegen ctx.rs doc-comment fix)

# Topic: correct stale CodegenArtifact.diagnostics reference in CodegenCtx doc

## Scope

One-line correction to the doc-comment on `CodegenCtx` in
`mock/crates/vehje-codegen/src/ctx.rs:14-19`. The comment still
references the retired `CodegenArtifact.diagnostics` field:

> Targets should not mutate anything through the context,
> diagnostics come back via the `CodegenArtifact.diagnostics` field
> on the return value.

The Round-5d codegen redesign retired that field. Targets receive
two caller-owned sinks (`&mut dyn ByteEmitter` +
`&mut dyn DiagnosticSink<Diagnostic>`); the artifact carries only
the `ArtifactKind`. The doc-comment lags behind the code.

Caught by the senior PR review on round 202605110300 (PR #34).
Cleanest fix is a fresh round; mockspace's source-edit gate requires
a CL even for doc-comment-only changes.

## Decisions

1. **Doc-comment-only edit; no semantic change.** The struct, its
   `impl`, and the function signatures are unchanged. Only the
   prose in the `///` doc-comment block flips to describe the
   current contract.

2. **Same PR as round 202605110300.** The senior reviewer
   explicitly said "do not let the stale comment sit; either fix
   it inline or open a follow-up before #413 closes." Inline is
   the lower-friction option; the round is small enough to clear
   a one-comment fix without dragging the merge.

## Cross-references

- Round 202605110300 (vehje-codegen test rehab): the parent PR.
- PR #34 senior review (agent ID `af3b622b1289c97ba`): the finding
  that surfaced this discrepancy.
- Task #413: vehje-codegen test rehab (the parent task this round
  closes alongside #34).

## Recorded

2026-05-10. Doc-comment-only mechanical correction.
