# Source changelist: Core binders become Sym

**Round:** 202607242351
**Phase:** IMPL
**Scope:** flip the three Core binder positions to `Sym`, declare `BinderDomain`, wire the `hilavitkutin-sym` dependency, and thread the new type through every consumer. Everything keeps compiling; the `Anf`/`MacroExpand` no-ops are untouched.

## Dependency wiring

### CHANGE: workspace + vehje-ir gain `hilavitkutin-sym`
Files: `mock/Cargo.toml` (add `hilavitkutin-sym = { git = "ssh://git@github.com/orgrinrt/hilavitkutin.git", branch = "dev" }` to `[workspace.dependencies]`), `mock/crates/vehje-ir/Cargo.toml` (add `hilavitkutin-sym.workspace = true`).
Verification: `grep -q hilavitkutin-sym mock/crates/vehje-ir/Cargo.toml` and `cargo check -p vehje-ir` resolves it.

## vehje-ir

### CHANGE: `Node::Var` FROM `Var(Str)` TO `Var(Sym)`
File: `mock/crates/vehje-ir/src/node.rs`
### CHANGE: `Node::Let.name` FROM `Str` TO `Sym`
File: `mock/crates/vehje-ir/src/node.rs`
### CHANGE: `Node::Lambda.param` FROM `Str` TO `Sym`
File: `mock/crates/vehje-ir/src/node.rs`
`Literal::Str(Str)` and `Node::Project.key: Str` are unchanged. `use hilavitkutin_sym::Sym`.
Verification: `grep -q 'Var(Sym)' mock/crates/vehje-ir/src/node.rs && grep -q 'key: Str' mock/crates/vehje-ir/src/node.rs`.

### CHANGE: marker `BinderDomain` CREATE
File: `mock/crates/vehje-ir/src/binder.rs` (new module)
`pub struct BinderDomain;` with `impl Domain for BinderDomain { const KIND: SymKind = SymKind::from_raw(0b001); }` and `impl GenerativeDomain for BinderDomain {}`. Re-exported from `lib.rs`.
Verification: `grep -q 'pub struct BinderDomain' mock/crates/vehje-ir/src/binder.rs`.

### CHANGE: builder `var`/`let_`/`lambda` FROM `Str` binder params TO `Sym`
File: `mock/crates/vehje-ir/src/builder.rs`
`var(name: Sym)`, `let_(.., name: Sym, ..)`, `lambda(param: Sym, ..)`. `project(key: Str)` unchanged. Source-name callers widen via `Str::as_sym`.
Verification: `grep -q 'name: Sym' mock/crates/vehje-ir/src/builder.rs`.

### CHANGE: `lib.rs` re-export `Sym`, `BinderDomain`
File: `mock/crates/vehje-ir/src/lib.rs`
Re-export `Sym` (from hilavitkutin-sym, for consumer convenience) and `BinderDomain`.
Verification: `grep -q 'BinderDomain' mock/crates/vehje-ir/src/lib.rs`.

### CHANGE: `hash.rs` folds `Sym` at binder positions
File: `mock/crates/vehje-ir/src/hash.rs`
The structural fold reads the binder identity; folding a `Sym`'s 32-bit `to_bits` is the same operation as a `Str`'s. Mechanical type update where a binder value is folded.
Verification: `cargo check -p vehje-ir`.

## Consumers (thread the new type)

### CHANGE: vehje-lower / vehje-resolve / vehje-typecheck / vehje-codegen / vehje-runtime-abi / vehje construct+match binders as `Sym`
Files: `mock/crates/vehje-lower/src/lib.rs` (`structurally_equal` binder `==` stays integer equality, now on `Sym`), `mock/crates/vehje-resolve/src/lib.rs`, `mock/crates/vehje-typecheck/src/lib.rs`, `mock/crates/vehje-codegen/src/lib.rs`, `mock/crates/vehje-runtime-abi/src/encode.rs`, `mock/crates/vehje/src/lib.rs`. Mechanical: every `Node::Var`/`Let.name`/`Lambda.param` construction takes a `Sym` (source names widen via `Str::as_sym`), every match binds a `Sym`. `Project`/`Literal::Str` sites unchanged.
Verification: `cargo check` on the whole workspace is clean.

## Tests (fail-first, strict)

### CHANGE: vehje-ir binder tests CREATE
- A minted `BinderDomain` `Sym` (kind `0b001`) is never equal to a source-name `Sym` (a `str_const!` widened via `as_sym`, kind `0b000`); assert the kinds differ (disjointness by domain, the capture-safety boundary).
- The builder accepts both a widened source `Str` and a minted `Sym` at `var`/`let_`/`lambda`.
- The structural hash of a `Var` built from a source name is unchanged from the pre-flip value (byte-compatibility of the `Str`->`Sym` widening).
Verification: `cargo test -p vehje-ir`.
