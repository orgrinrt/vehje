# Src changelist: split the typecheck tests

**Date:** 2026-07-25
**Phase:** IMPL
**Doc CL:** `202607252300_changelist.doc.lock.md`

## CHANGE: the `tests` module MOVES to a sibling file

File: `mock/crates/vehje-typecheck/src/lib.rs` and a new `mock/crates/vehje-typecheck/src/tests.rs`
FROM an inline `#[cfg(test)] mod tests { ... }` at the end of the crate root
TO `#[cfg(test)] mod tests;` with the body in the sibling, which is the shape `vehje-lower` and
`vehje-runtime-driver` already use for the same reason.
Verification: `cargo test -p vehje-typecheck` reports the same test count as before the move, and
`cargo mock` push validation reports the crate root under the 400-line gate.

## Not in this round

Anything else. The round moves lines and changes no behaviour.
