# Doc changelist: align the crate docs to the vehje canon

**Round:** canonical consolidation (`202607241545_topic.the-vehje-canon.md`).
**Phase:** DOC.
**Scope:** correct the drift the canon names, augment the crate docs with the canon's frontier where they under-state it, add the canon's new standing principles, and clear the one blocking undocumented-type gate. The doc set was built in prior rounds and is broadly aligned; this is a focused correction-and-augmentation, not a rewrite.

The canon is the oracle. Where the shipped source lags the design, the DESIGN docs state the design and label the state; they do not narrow the design to what ships. Backticked type names in `DESIGN.md.tmpl` must exist in source (the design-doc-source-mismatch lint); concepts not yet shipped are stated in prose or in `BACKLOG.md.tmpl`, unbackticked.

## Gate clear (blocking; do first)

### `mock/crates/vehje-typecheck/SHAME.md.tmpl`
Add a `## WithEffect` entry (50+ words). `WithEffect` is a test-only `FamilyCheck` fixture (a function-local struct in a `check_with` test at `vehje-typecheck/src/lib.rs`), used to exercise the Raw-node effect-join path where a family declares an effect through the hook. It is not a design surface and does not belong in `DESIGN.md.tmpl`; the entry documents why the undocumented-type lint fires on it and records that it is a test fixture, satisfying the lint without misfiling a test helper as a design type. This clears the `[undocumented-type] WithEffect BLOCKS BUILD AND PUSH` gate.

## Corrections the canon names

### `mock/crates/vehje-codegen/DEEPDIVE_OUTPUT_SPECTRUM.md.tmpl`
- Correct the flat "plain switch is fastest, tail-call threading roughly 1.7x slower" to the tier-scoped form: a plain switch is fastest inside straight-line blocks for the lean vocabulary, and preserve-none tail-threading wins at the CFG terminator transfers on real control flow (loops and branches, the trace cell near 0.44x of switch). Switch inside straight-line blocks, threading at the terminators. Keep the op-count reasoning; drop the blanket "measured-and-rejected" framing.
- Confirm native is stated as a range (roughly 1.5x to 2.0x over a good interpreter for scalar code, growing on branchy programs, 10x reserved for vectorisation), with direct instruction selection as the native point for the short-lived residuals the authoring majority produces and copy-and-patch de-prioritised (not deleted). Not a pinned scalar, not a privileged tier.

### `mock/crates/vehje-ir/DEEPDIVE_ARENA_AND_HASH.md.tmpl` (and `DESIGN.md.tmpl` if the boundary is stated there)
- State the identity boundary explicitly: within-stage identity is the interner-local structural (word-fold) hash; cross-stage and cross-artifact identity is the byte-image hash. The word-fold is not the cross-artifact identity. (Source note for the later src round: the `hash.rs` header still names the manifest as a word-fold consumer, which its own body and `vehje-runtime-gen` contradict; that header fix is owed to the src side, tracked in the canon Section 11.)
- Add the arena strategy as the canon states it: the framework adapts to N host-provided allocation spaces (single caller-lent arena the default hypothesis), the count a bench-decided fork, host-configurable once a win is confirmed, with the `NodeRef` branding decision riding the same fork. State in prose or BACKLOG (unshipped), unbackticked.
- Note the `Handle` representation and CR1 scope: bounded multi-shot by re-execution of a pure defunctionalised continuation is demonstrated (no-alloc); reinstatement without heap and multi-shot under other effects are open; the continuation representation is a bench-decided fork. Reflect the deferred `Handle` clause encoding.

### `mock/crates/vehje-lower/DESIGN.md.tmpl` (and `DEEPDIVE_LOWERING.md.tmpl`)
- State the IR-to-IR expansion as a catamorphism: macro expansion and constant folding are IR-to-IR passes that create nodes (not redirecting rewrites), iterated. Recursive macros are supported; termination is binding-time well-foundedness in three layers (stage well-foundedness; a within-stage per-operation productivity condition carried as signature data; a budget-as-diagnostic distinct from passthrough), with an always-on host-configurable depth-cap safety backstop (default 255) and static cyclicity detection layered above it as the primary mechanism where feasible. Reflect that `Anf` and `MacroExpand` need node creation.

### `mock/crates/vehje-runtime-abi/DESIGN.md.tmpl`
- State the CFG-of-blocks residual as the designed residual shape whose lowering from `Node` into blocks is owed (the `Block` / `Function` / `BlockTable` surface is the design; the lowering is downstream of `Anf`). Label it as design-with-owed-lowering, not as shipped.

### `mock/crates/vehje-runtime-gen/DESIGN.md.tmpl`
- State the certified-generation perimeter: rustc certifies the emitted data's well-formedness, inclusion, and grading; the Zig compiler certifies the comptime specialisation type-checks against that data; neither certifies semantic preservation of a lowering, and the Zig engine's evaluation logic, the C ABI marshalling, the byte-image hash, and the Rust-to-Zig emitter seam (until the lens projection lands) remain trusted. Cross-artifact identity is the byte-image hash. State the emitter seam as trusted today with the lens projection owed.

### `mock/crates/vehje-fixpoint/DESIGN.md.tmpl`
- Re-justify the relational engine on lease inference and load verification alone (the earlier justification that leaned on equality saturation as a live multi-shot consumer is retired; eqsat is parked). Whole-column evaluation beats delta semi-naive on realistic shallow graphs.

### `mock/crates/vehje-typecheck/DESIGN.md.tmpl`
- Add generative freshness and macro hygiene as a check concern: generative discharge is fresh by construction (capture structurally impossible because resolve runs after expansion), bounded by a host-lent budget, exhaustion a named diagnostic; one discipline with multi-shot-frame freshness. State: the enforcement (and the red capturing-macro test) is owed. Reference the effect-subtraction obligation for a handled operation under multi-shot resumption.

## New standing principles (top-level)

### `mock/PRINCIPLES.md.tmpl`
- Augment "Strict by default": strictness pairs with first-class diagnostics, so a refusal is a guide, not a wall.
- Add "Diagnostics are first-class": every error is informative and carries an actionable suggestion with span links that integrate with an LSP; this is a standing requirement, not a nicety.
- Add "Measure, and trust the measurement only as far as it proves": a which-is-faster or which-shape-is-cheaper fork is resolved by building candidates and measuring under the harness, not by first principles or prior-art authority; the data is trusted for exactly what its cell measured, the current benches are rudimentary and improved over time, and nothing is extrapolated past what a cell proves.
- Augment "Framework, not a language": the consumer census is a needs-signal the framework anticipates and matches, not a design target; the design is driven by the benches, the prior art, and the maximal shape, never tailored to a specific consumer.

### `mock/DESIGN.md.tmpl`
- Reflect the assurance-verb register (proven is a checked theorem, demonstrated is a sketch outcome, measured is a bench cell, intended is design) and the trusted-base honesty (name what the two certifications cover and what remains trusted), pointing at the canon for the full statement. Only if the top-level DESIGN carries a certified-generation or assurance section; otherwise a one-paragraph addition.

## Not in this doc CL (owed to later rounds, per the canon Section 11)
- Source changes (the graded-spine wiring, `fold_core` catamorphism, recursive-macro terminator, N-buffer bench, CR1 representation bench, CFG lowering, check-to-signature wiring, the `hash.rs` header fix): the src CL and the subsequent implementation rounds.
- The consolidation-topic corrections in `202607240100` and the second-direction audit: a separate doc round after this one.
