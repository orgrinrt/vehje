**Date:** 2026-04-25
**Phase:** DOC
**Scope:** Complete the clause to vehje rename across all non-frozen, non-deferred surfaces.
**Source topics:** none. Mechanical follow-up to round 202604300000 (#214) where the rename was started but ~356 prose sites and the runtime-zig surface were missed.

# Round goal

Round 202604300000 renamed the clause repo identity to vehje at the crate level (Cargo `[package] name`, build artefact names, top-level READMEs, and most prose). It deliberately deferred two categories: (a) the `mock/crates/clause-*/` directory rename (blocked by mockspace src-CL phase-gate, tracked under mockspace #41), and (b) the ABI type name rename (`ClauseResult`, `ClauseRuntime`, `ClauseDiagnostic`, `ClauseDiagnosticKind`), which crosses the Rust to Zig boundary and needs a coordinated cross-repo round.

Both deferrals stand. This round closes the rest: prose drift in templates, comments, source-level identifiers that are not the four ABI types, and the runtime-zig surface (which round 202604300000 left untouched).

## Out of scope (still deferred)

- `mock/crates/clause-*/` directory paths. Stays until mockspace #41 unblocks src-CL semantics.
- `ClauseResult`, `ClauseRuntime`, `ClauseDiagnostic`, `ClauseDiagnosticKind` and their associated function names (`vehje_runtime_execute` is fine, `clause_runtime_execute` would not be, but the four type names stay until the cross-repo round). Tracked separately.
- `clausewitz` (game grammar surface, retained as-is).
- `pre-clause` historical research dir.
- `mockspace.toml` `[forbidden-imports]` scopes referencing `clause-ir`, `clause-lex`, etc. These match actual directory names; they update only when the directories rename.
- `mock/Cargo.toml` workspace member path strings. Same reason.
- Any `mock/research/` and `mock/design_rounds/` content. Frozen historical artefacts.

## In scope (this round addresses)

Prose drift in:

- `mock/PRINCIPLES.md.tmpl`
- `mock/DESIGN.md.tmpl`
- `mock/agent/MAIN.md.tmpl`
- `mock/agent/hooks/no-alloc-guard.sh.tmpl`
- `mock/agent/hooks/no-bare-primitive-guard.sh.tmpl`
- `mock/agent/hooks/no-game-vocabulary-reminder.sh.tmpl`
- `mock/agent/hooks/no-python-port-literal-guard.sh.tmpl`
- `mock/agent/rules/implementation.md.tmpl`
- `mock/agent/rules/writing-style.md.tmpl`
- `mock/agent/skills/lint-rules/SKILL.md.tmpl`
- `mock/crates/clause-codegen/BACKLOG.md.tmpl`
- `mock/crates/clause-codegen/DESIGN.md.tmpl`
- `mock/crates/clause-lex/BACKLOG.md.tmpl`
- `mock/crates/clause-runtime-abi/BACKLOG.md.tmpl`
- `mock/crates/clause-runtime-abi/DESIGN.md.tmpl`
- `mock/crates/clause-runtime-tests/BACKLOG.md.tmpl`
- `mock/crates/clause-runtime-tests/DESIGN.md.tmpl`
- `mock/runtime-zig/BACKLOG.md`
- `mock/runtime-zig/DESIGN.md.tmpl`
- `mock/runtime-zig/README.md.tmpl`

## What changes

In every file above, replace standalone `clause` with `vehje` and `Clause` with `Vehje` in prose. Preserve:

- Backticked identifiers that are still valid: `clause-runtime-abi` (the dir), `clause-ir`, `clause-lex` (dirs), `Clause.toml` (manifest filename, deferred to a separate decision), `clausewitz` (game surface).
- Function/identifier names beginning with `Clause` that are the four deferred ABI types.
- File path references that point at still-named directories.

Add the standalone `vehje-runtime-abi` etc. names where they appear in prose alongside the dir-path forms. The dir paths read `mock/crates/clause-runtime-abi/`; the crate identity reads `vehje-runtime-abi`. Both forms coexist until the dirs rename.

## Comparison to deprecated changelist

Not deprecating an earlier CL in this round. This is a follow-up to round 202604300000 which closed normally. Backticked types referenced in 202604300000's `*.doc.lock.md` and `*.src.lock.md`: `CompilerSchedule`, `ScheduleError`, `ClauseResult`, `ClauseRuntime`, `ClauseDiagnostic`, `ClauseDiagnosticKind`, `vehje_runtime_execute`, `arvo::USize`, `arvo::Cap`. None are renamed by this round; the four `Clause*` types are the deferred ABI rename.
