# Doc CL: State labels and intended-mood re-tensing (second-direction audit)

**Round:** `202607241730_topic.doc-honesty-second-direction-audit.md`.
**Phase:** DOC.
Doc-template-only. Each edit adds a "State:" label or re-tenses a present-tense overclaim to the intended mood, per the canon's assurance-verb discipline. No design claim's substance changes; only its shipped-vs-intended status is made explicit. Backticked type names must exist in source (design-doc-source-mismatch lint); the labels reference existing types.

## `mock/crates/vehje-lower/DESIGN.md.tmpl` (audit 7, 8)
- The `MacroExpand` bullet: re-tense "its effect is discharged here, so the residual carries no unexpanded macro" to the intended mood (it is designed to discharge here; `apply` is a no-op today) with a State label naming the fold_core catamorphism as the owed prerequisite.
- The "Recursive macros are supported" paragraph: re-tense to "Recursive macros are designed to be supported" and add a State label: unbuilt today (no depth-cap constant, no productivity-condition field on `vehje-signature::Operation`, no cyclicity sketch, `MacroExpand::apply` a no-op); the mechanism is owed.

## `mock/crates/vehje-schedule/DESIGN.md.tmpl` (audit 15, 16, open-q 1)
- Add a State label at the top of the crate design: the crate is a contingent, possibly-dissolving mechanism. Its `Pass` / `PassDag` / `Schedule` types are intentionally stubs today, not fleshed to match this doc, pending the unification question (do family passes turn out to be handler instances of the compile-stage effect discipline, in which case the scheduler folds into the orchestrator, per the crate's own "narrowed remit" note and the canon's handler discipline). Do not read the DAG description as shipped; it is the design the crate would take if the unification resolves against dissolution. Re-tense the `Pass`/`PassDag`/`Schedule` bullets to the intended mood or label them owed.

## `mock/crates/vehje-codegen/DESIGN.md.tmpl` (audit 13)
- Re-frame the "Module layout" section as the intended layout (not a present-tense file list): only `lib.rs` exists today; `compose.rs` (compile-time target composition) is owed. Add a State label that target composition is unbuilt, tracked as the distribution-composition item.

## `mock/crates/vehje/DESIGN.md.tmpl` (audit 14)
- Add a State label on the distribution-composition entry point: designed, unbuilt today (the crate exports one reference target and a `run` that stops after resolve); target composition across a set of targets is owed.

## `mock/crates/vehje-fixpoint/DESIGN.md.tmpl` (audit 9, 10, 11, 12)
- Re-tense the `Engine::evaluate` dual-strategy sentence to the intended mood with a State label: whole-column evaluation ships; the semi-naive-delta strategy is owed (the crate's own `engine.rs` FIXME names this).
- Add a State label that the engine has zero consumers today: it is built and benched at the algorithm level, and wiring it to lease inference and load verification is owed (this carries the honest state for the canon's own unlabeled Section 4 claim, which is frozen and cannot be edited in place).
- Add a bench-provenance note: the 8M-node / 16M-edge / ~50 ms figure comes from a standalone Zig prototype (`reach.zig`), not from a harness run against the Rust `Engine`; the algorithm was benched, the shipped crate was not directly benched.

## `mock/crates/vehje-typecheck/DEEPDIVE_GRADED_SPINE.md.tmpl` (audit 19, 20)
- Add a per-axis State label to the grade-computation claim: `effect` and `lease` are computed via the join fold today; `binding` and `assurance` are hard-coded constants (`Knowledge::empty()`, `Assurance::Unclaimed`), so two of four axes are computed and two are owed.
- Re-tense "The differential test ships" to the intended mood with a State label: the `DifferentialCheck` contract type ships; the harness runner that drives a census corpus through both the reference and the specialised runtime is owed (its own FIXME says so).

## `mock/crates/vehje-runtime-driver/DESIGN.md.tmpl` (audit 22)
- Add a State label on the crate's identity: the `std` role and the dlopen of the runtime artifact are designed and owed (dispatch ignores its argument today, `libloading` is a BACKLOG item, `std` is an empty feature); the crate is `no_std` in source today.

## `mock/crates/vehje-runtime-gen/DESIGN.md.tmpl` (audit 4)
- Tighten the `Manifest` bullet's first clause so the "of each slice" part carries its owed status inline (per-slice hashes are owed; the interim is a whole-package byte hash), rather than only in the later sentence.

## Not in this doc CL
Source doc-comment overclaims (`vehje/src/lib.rs` distribution-composition, `vehje-fixpoint/src/lib.rs` "semi-naive" crate doc, `vehje-runtime-driver/src/lib.rs` module doc) land in a later src CL. The frozen canon topic is not edited. No mechanism is built (implementation rounds).
