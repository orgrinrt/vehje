# Src CL: no source changes (doc-only round)

**Round:** `202607241730_topic.doc-honesty-second-direction-audit.md`, against the locked doc CL.
**Phase:** IMPL.

This round is doc-template-only: it adds "State:" labels and re-tenses present-tense overclaims to the intended mood across the crate DESIGN and DEEPDIVE docs, so a reader can tell shipped from intended. No source changes.

The source-side work the audit surfaced is deliberately deferred to the implementation rounds, each its own mockspace round:

- The `vehje-lower` IR-to-IR expansion (the fold_core catamorphism so `Anf` and `MacroExpand` create nodes, then the recursive-macro terminator with a productivity-condition field on `vehje-signature::Operation`).
- Wiring `vehje-fixpoint::Engine` to lease inference in `vehje-typecheck` (closing the no-consumers gap).
- The graded-spine binding-time and assurance grade computation (two of four axes are constants today).
- The differential-harness runner in `vehje-runtime-gen`.
- The CFG-of-blocks lowering from `Node` into blocks.
- The distribution-composition entry point (and the `vehje/src/lib.rs` module-doc-comment overclaim it carries).
- The `vehje-runtime-driver` dlopen and `std`-gated embedding.
- The source doc-comment overclaims noted in the doc CL but out of this round's doc-only scope: `vehje-fixpoint/src/lib.rs`'s "semi-naive" crate doc, `vehje-runtime-driver/src/lib.rs`'s module doc, and `vehje/src/lib.rs`'s distribution-composition line. These land with their crate's implementation round or a dedicated source-doc-comment pass.
- The `vehje-schedule` build only after the dissolution question resolves (whether family passes are handler instances of the compile-stage effect discipline, in which case the scheduler folds into the orchestrator); a design call, not a build to schedule blindly.
