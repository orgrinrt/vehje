# Src Changelist: clause R1 — scaffold creation (design-only round)

**Date:** 2026-04-26
**Phase:** SRC
**Scope:** workspace-level scaffolds + 7 new empty crate members.
**Doc CL:** 202604261500_changelist.doc.md

## Deliverables

R1 is a design-only round. No Rust algorithmic source ships in it.
The "src" work is exclusively workspace scaffolding per the doc CL:

1. Seven empty crate directories under `mock/crates/`:
   `clause-ir`, `clause-lex`, `clause-syntax`, `clause-resolve`,
   `clause-typecheck`, `clause-schedule`, `clause-codegen`.
2. Per-crate files:
   - `Cargo.toml` with workspace package + phase-appropriate
     workspace deps.
   - `build.rs` running `mockspace::bootstrap::bootstrap_from_buildscript()`.
   - `src/lib.rs` with the nuked-marker (no Rust items).
   - `README.md.tmpl` (3-10 line crate summary).
   - `DESIGN.md.tmpl` (role, scope, non-scope, dependencies,
     provisional module layout, deferred items, sources).
3. `mock/Cargo.toml` workspace members + path deps extended.
4. `mock/mockspace.toml` layers populated (IR / Lex / Syntax /
   Resolve / TypeCheck / Schedule / CodeGen / CLI).

None of the seven new crates ship real Rust items. Each one's
impl round (Phase 3 / 4b / 5b / 5c / 5d / schedule TBD) fills in
its src per its DESIGN.

## Out of scope

- Any `.rs` item definitions — impl rounds provide those.
- Zig runtime + C ABI — R2 (Task #28).
- Extension-point contract — R3 (Task #29).
- Validator framework — R4 (Task #30).

## Verification

- `cargo check --workspace` compiles the eight nuked crates.
- `cargo mock check` reports DOC phase green; the R1 round closes
  after this src CL locks, leaving each crate in a testable nuked
  state ready for its own round.

## Comparison to deprecated changelist

No deprecated predecessor for R1 content. The seed CL
(202604191301) was the initial-seed record and is orthogonal; it
recorded the Python-to-repo import, not any R1–R4 decisions.

## Impl-time decisions (pre-committed)

1. **No `BACKLOG.md.tmpl`** in the new crates this round — each
   crate's BACKLOG lands with its impl round, when concrete items
   to defer surface.
2. **Binary crate `clause` retains its placeholder DESIGN** from
   the bootstrap commit. R1's doc CL expanded the provisional
   compiler-split section of `mock/crates/clause/DESIGN.md.tmpl`
   is deferred to `clause` binary's own round (post-lex/syntax;
   the binary ties things together).
3. **`clause-ir` has no dedicated impl round** in the task board.
   Its types fill in as dependent phases (starting with clause-lex
   at Task #31) need them. First phase round adds the types it
   needs; later rounds extend.
