**Date:** 2026-04-25
**Phase:** SRC
**Scope:** Source-side ABI type rename to match the locked doc CL.

# Source changes

## `mock/crates/vehje-runtime-abi/`

The four ABI types defined here flip name. Mechanical sed pass across:

- `src/lib.rs`. Module re-exports and `extern "C"` declarations.
- `src/result.rs`. `enum ClauseResult` to `enum VehjeResult` plus all field uses.
- `src/handle.rs`. `struct ClauseRuntime` to `struct VehjeRuntime` plus the `extern "C" fn vehje_runtime_*` signatures consuming it.
- `src/exports.rs`. `extern "C" fn vehje_runtime_new() -> *mut ClauseRuntime` etc. retypes to `*mut VehjeRuntime`.
- `src/diagnostic.rs`. `struct ClauseDiagnostic` and `enum ClauseDiagnosticKind` to the new names.
- `tests/abi_types.rs` and `tests/exports.rs`. Test sites referencing the renamed types.

## `mock/crates/vehje-runtime-driver/src/error.rs`

References the diagnostic type for error mapping. Single-occurrence rename.

## `mock/crates/vehje-runtime-tests/`

- `src/lib.rs`. The `pub use vehje_runtime_abi::{AbiSpan, ClauseDiagnostic, ClauseDiagnosticKind, ClauseResult}` re-export gains the new names.
- `tests/smoke.rs`. Smoke test references.

## `mock/runtime-zig/src/runtime.zig`

The four `pub const CLAUSE_RESULT_<X>: i32 = N;` constants flip to `pub const VEHJE_RESULT_<X>: i32 = N;`. The single in-body return `CLAUSE_RESULT_ERR` flips to `VEHJE_RESULT_ERR`.

## `mock/runtime-zig/build.zig`

No source content changes from this round (the build script does not reference the ABI types).

# Out of scope

After this round closes, the only `Clause`/`CLAUSE_` text remaining in the vehje repo should be:

- frozen historical artefacts under `mock/research/` and closed `mock/design_rounds/<round>/` subdirectories
- legitimate `Clausewitz` game-name references
- `Clause.toml` (manifest filename, deferred decision)
- English / Rust syntax usages of "clause" (e.g., `where clause`)

The `clause-dev` workspace directory name and the unrelated parked `clause` crates.io name are out of scope.

# Comparison to deprecated changelist

Not deprecating. The eight identifier renames documented in the doc CL are the full scope of source-side changes. The earlier round 202604300000 documented these as deferred; this round closes that out.
