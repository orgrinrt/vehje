**Date:** 2026-05-29
**Phase:** SRC
**Round:** 202605291505 (enable notko const + try_trait_v2)

Build-config: align vehje's notko dep with the stack's feature set and enable notko's core `Try` / `Residual` impls in vehje's build.

## CHANGE: notko dep features FROM (none, default-features=false) TO `["const", "try_trait_v2"]`
File: `mock/Cargo.toml`
Add `features = ["const", "try_trait_v2"]` to the notko workspace dependency (was `default-features = false` with no features list, so neither const nor try_trait_v2). `const` brings notko's const-trait fallibility surface in line with arvo/hilavitkutin; `try_trait_v2` transitively enables notko's `try_trait_v2_residual` rustc gate, compiling notko's fixed `Try` + `Residual` impls.
Verification: the notko line in `mock/Cargo.toml` carries `features = ["const", "try_trait_v2"]`; `cargo +nightly-2026-05-28 test --workspace` builds and passes.

## CHANGE: Cargo.lock notko + arvo pins advanced to merged dev fixes
File: `mock/Cargo.lock`
`cargo update -p notko -p arvo` pulls notko c2e5b19 (the Residual adaptation) and arvo 5776b61 (the Bool fix).
Verification: `cargo +nightly-2026-05-28 check --workspace` clean.
