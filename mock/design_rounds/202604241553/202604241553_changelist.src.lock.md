# Changelist (src): rename to `vehje`

**Date:** 2026-04-24
**Phase:** SRC
**Source topics:** `202604241553_topic.rename-to-vehje.md`
**Pairs with:** `202604241553_changelist.doc.lock.md`

## Src-side changes

### Directory renames (`git mv`)

- `mock/crates/clause` -> `mock/crates/vehje`
- `mock/crates/clause-codegen` -> `mock/crates/vehje-codegen`
- `mock/crates/clause-ir` -> `mock/crates/vehje-ir`
- `mock/crates/clause-lex` -> `mock/crates/vehje-lex`
- `mock/crates/clause-resolve` -> `mock/crates/vehje-resolve`
- `mock/crates/clause-runtime-abi` -> `mock/crates/vehje-runtime-abi`
- `mock/crates/clause-runtime-driver` -> `mock/crates/vehje-runtime-driver`
- `mock/crates/clause-runtime-tests` -> `mock/crates/vehje-runtime-tests`
- `mock/crates/clause-schedule` -> `mock/crates/vehje-schedule`
- `mock/crates/clause-syntax` -> `mock/crates/vehje-syntax`
- `mock/crates/clause-typecheck` -> `mock/crates/vehje-typecheck`

### `mock/Cargo.toml`

- Workspace `members` paths: `crates/clause*` -> `crates/vehje*`
- `[workspace.dependencies]` path-dep aliases: `clause-*` -> `vehje-*`

### `mock/crates/vehje*/Cargo.toml`

- `[package] name = "clause-*"` -> `"vehje-*"`
- Per-crate dependency declarations: `clause-*` -> `vehje-*`, `path = "../clause-*"` -> `path = "../vehje-*"`

### Source (`mock/crates/vehje*/src/**/*.rs`, `tests/**/*.rs`, `build.rs`)

Mechanical identifier renames:

- `use clause_ir::*` / `use clause_lex::*` / ... -> `use vehje_*::*`
- `clause::*` crate-root references -> `vehje::*`
- `extern crate clause` -> `extern crate vehje`
- Language-name prose in module docs (`//!`) and item docs (`///`) where unambiguous: `Clause language` / `Clause compiler` / `Clause runtime` / `Clause grammar` / `Clause keyword` / `Clause dependency` / `Clause source` / `Clause authoring` / `the Clause ` / `Clause.toml` -> `Vehje ...`
- C ABI symbol names: `clause_runtime_new` / `clause_runtime_free` / `clause_runtime_execute` / `clause_runtime_last_error` -> `vehje_runtime_*`
- Test-function names invoking those ABI symbols: `clause_result_*` / `clause_diagnostic_*` -> `vehje_*`
- CLI strings in `vehje/src/main.rs`: `` `clause` `` / `clause: unknown subcommand` / `usage: clause ...` -> `vehje`
- Dylib path in loader test: `libclause_runtime.dylib` -> `libvehje_runtime.dylib`

### `mock/Cargo.lock`

- Bump `mockspace-lint-rules` and `mockspace` pins from `e3f6f6d` to `acf2861` so the external lint pack (`mockspace-hilavitkutin-stack-lints`) can resolve `LintContext::should_skip_proc_macro_source_lint()`, added at `acf2861`.

## Comparison to deprecated changelist

Not applicable.

## What this changelist does not touch

- `mock/design_rounds/**` and `mock/research/**`. Historical artefacts.
- `mock/tasks/workflow/**`. Schema + history.
- `mock/mockspace.toml` `[lints.forbidden-imports]` `scope = "clause-*"` rule entries. Presented as a separate human-overseer diff.
- Generic-English uses of the word "clause" (Rust `where clause`, `trait clause` in prose). Grep-preserved.

## Verification

- `cargo check --workspace` green after all renames.
- `grep -rnE 'clause[-_]|\`clause\`|Clause\b' mock --include='*.rs' --include='*.toml' --include='*.tmpl' --include='*.md'` returns zero hits outside `mock/design_rounds/`, `mock/research/`, `mock/tasks/workflow/`, and `[lints.forbidden-imports]` scope entries.
