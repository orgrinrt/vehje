# Doc changelist: the value arena's state label

**Date:** 2026-07-25
**Phase:** DOC
**Topic:** `202607252000_topic.value-arena-state-label.md`

## `mock/runtime-zig/DESIGN.md.tmpl`

**Section: `## The value arena`.** Change `State: intended` to `State: shipped`, since the arena, its
constructors, the tree writer, the untrusted reader, and the compound marshalling all landed in round
202607251700 and `Project` reads records out of the arena as of 202607251900.

**Same section, the update-path paragraph.** Keep the description of what the code does in the present tense
and keep the uniqueness bit explicitly owed, so the section reads as a shipped arena shaped for an analysis
that does not exist yet, rather than as an unbuilt whole.

No other document changes. No source changes.
