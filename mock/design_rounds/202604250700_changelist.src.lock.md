**Date:** 2026-04-25
**Phase:** SRC
**Scope:** Source-only directory rename for the 11 mockspace crates and the supporting lint/path config.

# Source changes

## Directory renames (`mock/crates/`)

Eleven directories rename via `git mv`:

- `clause` to `vehje`
- `clause-codegen` to `vehje-codegen`
- `clause-ir` to `vehje-ir`
- `clause-lex` to `vehje-lex`
- `clause-resolve` to `vehje-resolve`
- `clause-runtime-abi` to `vehje-runtime-abi`
- `clause-runtime-driver` to `vehje-runtime-driver`
- `clause-runtime-tests` to `vehje-runtime-tests`
- `clause-schedule` to `vehje-schedule`
- `clause-syntax` to `vehje-syntax`
- `clause-typecheck` to `vehje-typecheck`

Each rename is a `git mv` of the directory; all child files (`Cargo.toml`, `src/`, `tests/`, `README.md.tmpl`, `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `SHAME.md.tmpl`) move along with the directory. No content edits inside the moved files are required by this round.

## `mock/Cargo.toml`

Update the workspace `members` list and the `[workspace.dependencies]` `path = "crates/clause-*"` entries to point at the new `crates/vehje-*` directories. Eleven member-list entries and ten dependency-path entries flip.

## `mock/mockspace.toml`

The `[forbidden-imports]` section references the old crate-directory names by scope (`clause-ir`, `clause-lex`, `clause-syntax`, `clause-runtime-abi`). These scope strings are matched against the renamed-directory paths, so each scope updates from `clause-*` to `vehje-*`. This is not a severity edit; the lint severities are unchanged.

## Auto-regenerated files

After the renames, `cargo mock` regenerates:

- `.claude/hooks/lint-forbidden-clause-*.sh` to `.claude/hooks/lint-forbidden-vehje-*.sh` (4 files)
- `.claude/rules/lint-forbidden-clause-*.md` to `.claude/rules/lint-forbidden-vehje-*.md` (4 files)
- `.github/instructions/lint-forbidden-clause-*.instructions.md` to `.github/instructions/lint-forbidden-vehje-*.instructions.md` (4 files)
- `.github/hooks/lint-forbidden-clause-*.sh` to `.github/hooks/lint-forbidden-vehje-*.sh` (4 files)
- `docs/CLAUSE_*_OVERVIEW.md` to `docs/VEHJE_*_OVERVIEW.md` (11 files)
- `docs/STRUCTURE.GRAPH.*` and `docs/mock_crate_deps.*` (graph artefacts)

These are mechanical regenerations; the round commits them as part of the rename.

# Out of scope

- ABI type names (`ClauseResult`, `ClauseRuntime`, `ClauseDiagnostic`, `ClauseDiagnosticKind`) and the corresponding zig `CLAUSE_RESULT_*` constants. Cross-repo Rust+Zig coordination round.
- `Clause.toml` manifest filename. Separate decision.
- Any remaining `clause`/`Clause` prose drift inside the renamed crate templates: the prior round (`202604250637`) covered the major templates; per-crate template content already says `vehje-*`. If any residual drift surfaces post-rename, it lands as a follow-up.

# Comparison to deprecated changelist

Not deprecating. Backticked types: none new in this CL beyond the directory paths. The deferred ABI types (`ClauseResult`, `ClauseRuntime`, `ClauseDiagnostic`, `ClauseDiagnosticKind`) are documented as untouched.
