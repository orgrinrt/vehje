---
round: 202605110100
phase: SRC
scope: vehje (lib-only)
---

# Src CL: must_use Maybe + arvo Round D facade

Library-side mechanical adoption. Five Rust files plus one Cargo.lock
update. No public API change; tests deferred.

## Files

- `mock/crates/vehje-lex/src/lexer.rs`
- `mock/crates/vehje-lex/src/tokenizers.rs`
- `mock/crates/vehje-syntax/src/parser.rs`
- `mock/crates/vehje-resolve/src/resolved.rs`
- `mock/crates/vehje-resolve/src/scope.rs`
- `mock/Cargo.lock`

## CHANGE: explicitly discard Maybe returns from `Cursor::bump*` in vehje-lex

File: `mock/crates/vehje-lex/src/{lexer,tokenizers}.rs`
Verification: `! grep -E '^\s+(self\.cursor\.|c\.)?bump_byte\(\);' mock/crates/vehje-lex/src/{lexer,tokenizers}.rs` returns no matches; `grep -c "let _ = .*bump_byte()" mock/crates/vehje-lex/src/{lexer,tokenizers}.rs` totals 18 matches across the two files (3 lexer + 15 tokenizers). The lexer source already had `self.cursor.bump().isnt()` wrapped via `if`, so only the bare `bump_byte()` discards needed update.

Before: `c.bump_byte();` / `self.cursor.bump_byte();`
After: `let _ = c.bump_byte();` / `let _ = self.cursor.bump_byte();`

## CHANGE: explicitly discard Maybe return from `Parser::bump` in vehje-syntax

File: `mock/crates/vehje-syntax/src/parser.rs:169`
Verification: `grep -n "let _ = self.bump();" mock/crates/vehje-syntax/src/parser.rs` returns one match.

Before: `self.bump();`
After: `let _ = self.bump();`

## CHANGE: replace `arvo::newtype::Bool` import with top-level `arvo::Bool` in vehje-resolve

Files: `mock/crates/vehje-resolve/src/{resolved,scope}.rs`
Verification: `! grep -rn "arvo::newtype" mock/crates/` returns no matches; `grep -rn "use arvo::{Bool" mock/crates/vehje-resolve/src/` returns at least the scope.rs match; `grep -rn "use arvo::Bool" mock/crates/vehje-resolve/src/` returns the resolved.rs match.

Before:
- `use arvo::newtype::Bool;` (resolved.rs:13)
- `use arvo::{newtype::Bool, USize};` (scope.rs:15)

After:
- `use arvo::Bool;` (resolved.rs:13)
- `use arvo::{Bool, USize};` (scope.rs:15)

The `arvo::newtype` module retired in arvo Round D. `Bool` is now re-exported from the top-level facade alongside `USize`.

## CHANGE: bump hilavitkutin / arvo / notko pins in mock/Cargo.lock

File: `mock/Cargo.lock`
Verification: `grep "hilavitkutin.git?branch=dev#" mock/Cargo.lock | head -1` shows commit `3c0d0d5d` (latest hilavitkutin dev including round 202605091700 + 202605110000); `grep "arvo.git?branch=dev#" mock/Cargo.lock | head -1` shows `0f5711c4` (arvo Round 1-8 substrate); `grep "notko.git?branch=dev#" mock/Cargo.lock | head -1` shows `b8509c9a` (notko #15 must_use round).

Mechanical `cargo update -p hilavitkutin-api` plus a fresh `cargo check` to synchronise the lock to the upstream `branch = "dev"` heads. No Cargo.toml change.

## Lock criteria

- `cargo check --manifest-path mock/Cargo.toml` green.
- `cargo mock --lint-only --commit` green.
- No `*.md.tmpl` diff (this round is library-source-only; doc CL is
  empty).
- Tests are NOT in scope; they have pre-existing rot tracked under a
  follow-up task (test rehab, vehje-side, sibling to hilavitkutin
  #399).
