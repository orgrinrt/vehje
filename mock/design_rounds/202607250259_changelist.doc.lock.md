# Doc changelist: wire run end to end

**Round:** 202607250259
**Phase:** DOC
**Scope:** vehje

## vehje/DESIGN.md.tmpl

### CHANGE: the per-program path no longer stops after resolve
Update the sentence that says `run` "stops after resolve" to describe the
wired path: `run` resolves, runs the graded check, mints the `Checked`
witness through the target's inclusion sets, and emits through the target.
Note that the cheap lowering integration and the runtime execution hand-off
remain the next gates.
