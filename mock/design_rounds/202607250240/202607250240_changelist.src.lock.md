# Source changelist: split the ANF catamorphism to a sibling module

**Round:** 202607250240
**Phase:** IMPL
**Scope:** move the ANF catamorphism out of vehje-lower/src/lib.rs so lib.rs
falls under the 400-line file-size gate. No behaviour change.

## CHANGE: the ANF catamorphism MOVES FROM `lib.rs` TO `anf.rs`
Files: `mock/crates/vehje-lower/src/lib.rs` (the `Anf` type, the
`normalize_term` / `flatten` / `atomize` / `atomize_list` / `normalize_list`
fold, the `record` / `dummy_ref` helpers, and the `BIND_CAP` / `ARG_CAP`
consts are removed and replaced by `mod anf;` plus `pub use anf::Anf;`),
`mock/crates/vehje-lower/src/anf.rs` (new; holds the moved code with its own
`use` imports and `use crate::Rewrite;`).
Verification: `grep -q '^mod anf;' mock/crates/vehje-lower/src/lib.rs && grep -q 'pub struct Anf' mock/crates/vehje-lower/src/anf.rs && cargo test -p vehje-lower` passes, and lib.rs non-blank-non-comment lines drop under 400.
