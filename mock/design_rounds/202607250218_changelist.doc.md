# Doc changelist: node-creating ANF catamorphism

**Round:** 202607250218
**Phase:** DOC
**Scope:** vehje-lower

## vehje-lower/DESIGN.md.tmpl

### CHANGE: the `Anf` bullet FROM "deferred no-op" TO "node-creating catamorphism"
Rewrite the `Anf` bullet (the "## Lowering" list) so it describes the shipped
shape: `Anf::apply` is a node-creating catamorphism that flattens a term to
flat A-normal form, minting fresh capture-safe binders via
`Generator<BinderDomain>` (kind `0b001`, never equal to a source name's
`0b000`), building inner-to-outer against the append-only arena with no
closures and no alloc, and returning the ANF root (`Isnt` on arena-full or
generator-exhaustion). Note the flat-not-nested normal form, the fixed-cap
operand scratch (with the pool-resident incremental-list primitive as the
follow-up that removes the arity cap), and that `Lower::lower` takes a
`&mut Builder` plus a `Generator<BinderDomain>` and returns the ANF root.
Drop the "deferred piece" / "no-op" framing.

No other template changes this round. The `MacroExpand` bullet keeps its
current "no consumer family defines a macro operation yet" framing (it stays a
no-op this round; the shared node-creating path it will use is the one `Anf`
lands here).
