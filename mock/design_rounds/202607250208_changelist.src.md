# Source changelist: split vehje-lower/lib.rs

**Round:** 202607250208
**Phase:** IMPL
**Scope:** move the vehje-lower test module to a sibling file so lib.rs
falls under the 400-line file-size gate. No behaviour change.

## CHANGE: vehje-lower test module MOVES FROM `lib.rs` TO `tests.rs`
Files: `mock/crates/vehje-lower/src/lib.rs` (the `#[cfg(test)] mod tests { ... }`
body is removed and replaced by `#[cfg(test)]\nmod tests;`),
`mock/crates/vehje-lower/src/tests.rs` (new; holds the moved module body as
`use super::*;` plus the tests).
Verification: `grep -q '^mod tests;' mock/crates/vehje-lower/src/lib.rs` and `cargo test -p vehje-lower` passes, and lib.rs non-blank-non-comment lines drop under 400.
