**Date:** 2026-04-25
**Phase:** DOC
**Scope:** Rename the four `Clause*` ABI type names + four `CLAUSE_RESULT_*` zig constants. The Rust ABI and the Zig runtime live in the same repo; the prior "cross-repo coordination round" framing was wrong.

# Round goal

Drop the last `Clause`/`CLAUSE_` identifiers from the vehje repo. The remaining occurrences sit at the FFI boundary in `mock/crates/vehje-runtime-abi/` and `mock/runtime-zig/`. Both sides are in this repo, so a single coordinated rename inside one PR is feasible. After this round, the only `Clause*` text left should be in frozen historical artefacts (`mock/research/`, `mock/design_rounds/<closed>/`) and the legitimate `Clausewitz` game-name references.

## Type renames

- `ClauseResult` to `VehjeResult` (Rust enum, Zig refers to it in prose only)
- `ClauseRuntime` to `VehjeRuntime` (Rust opaque handle type)
- `ClauseDiagnostic` to `VehjeDiagnostic` (Rust diagnostic struct)
- `ClauseDiagnosticKind` to `VehjeDiagnosticKind` (Rust enum)

## Zig constant renames

- `CLAUSE_RESULT_OK` to `VEHJE_RESULT_OK`
- `CLAUSE_RESULT_ERR` to `VEHJE_RESULT_ERR`
- `CLAUSE_RESULT_NULL_HANDLE` to `VEHJE_RESULT_NULL_HANDLE`
- `CLAUSE_RESULT_INVALID_INPUT` to `VEHJE_RESULT_INVALID_INPUT`

# Doc-template changes

The `DESIGN.md.tmpl` and `BACKLOG.md.tmpl` templates under `mock/crates/vehje-runtime-abi/`, `mock/crates/vehje-runtime-tests/`, and `mock/runtime-zig/` reference the old type and constant names. Each occurrence in those templates flips to the new name. Mechanical: the `Clause` prefix becomes `Vehje`, the `CLAUSE_` prefix becomes `VEHJE_`. No structural changes to any template.

# Comparison to deprecated changelist

Not deprecating. Backticked types touched: the eight names listed above, all in their current (`Clause*` / `CLAUSE_*`) form before the round and their new (`Vehje*` / `VEHJE_*`) form after. The earlier round 202604300000 documented these as deferred ("ABI type-name rename round, cross-repo Rust+Zig coordination round"). This round closes that out.
