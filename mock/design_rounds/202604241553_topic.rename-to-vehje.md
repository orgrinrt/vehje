# Topic: rename `clause` to `vehje`

**Date:** 2026-04-24
**Phase:** TOPIC
**Scope:** Repo-wide crate rename, mechanical.

## Current state

Top-level crate name is `clause`, sub-crates are `clause-{ir,lex,syntax,resolve,typecheck,schedule,codegen,runtime-abi,runtime-driver,runtime-tests}`. Identifier paths follow suit (`clause_ir`, `clause_lex`, etc.). The `clause` name is taken on crates.io by an unrelated project; this repo has no prior publication under that name.

## Changes proposed

Rename to `vehje` / `vehje-*` across the repo. Scope: directory paths, Cargo.toml package and workspace-member entries, `clause_*` module identifiers in source, `clause::` crate-root references, `clause_runtime_*` C ABI symbol names, `Clause` language-name prose in live doc templates, `Clause.toml` manifest file name in docs, the top-level `README.md`, and `mock/agent/**/*.tmpl` agent instructions.

Out of scope for this round:

- `mock/design_rounds/**` and `mock/research/**`. Historical artefacts; they record conversations about the language at the time they were written and are not retroactively rewritten.
- `mock/tasks/workflow/**`. Task-workflow schema + history, not documentation.
- `mockspace.toml` `[lints.forbidden-imports]` `scope = "clause-*"` rule entries. These lint scope strings need updating for the lint to keep matching the renamed crates (otherwise silent downgrade), but they live inside a `[lints.*]` table, which workspace rules reserve for human-overseer edits. Presented as a separate diff for explicit approval.

Mockspace-lint-rules git-dep pin bumps from `e3f6f6d` to `acf2861` in `mock/Cargo.lock` because the external lint pack (`mockspace-hilavitkutin-stack-lints`) calls `LintContext::should_skip_proc_macro_source_lint()`, added in `acf2861`.
