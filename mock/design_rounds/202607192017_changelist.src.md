# Src Changelist: migrate vehje to the launcher model

**Date:** 2026-07-19
**Phase:** SRC
**Scope:** repo-wide build/tooling.
**Doc CL:** 202607192017_changelist.doc.lock.md
**Supersedes:** none.

## Deliverables

### Remove the old proxy + build.rs bootstrap

1. Delete `mock/crates/*/build.rs` (11 files). Each only called
   `mockspace::bootstrap::bootstrap_from_buildscript()`, the dissolved proxy's
   self-install.
2. Remove `[build-dependencies] mockspace.workspace = true` from those 11
   crates' `Cargo.toml`.
3. Remove the `mockspace` entry from `mock/Cargo.toml` `[workspace.dependencies]`.
4. Delete `.cargo/config.toml` (the `cargo mock` proxy alias).

### Adopt the launcher pin

5. Move `mock/mockspace.toml` to the repo-root `mockspace.toml`, adding
   `mock_dir = "mock"` and `mockspace_branch = "dev"` at the top. The single
   config a repo now has.
6. Pin the `[lint-crates]` stack-lints pack to `branch = "dev"` so its
   lint-rules matches the dev-pinned engine.
7. `.gitignore`: adopt the launcher/engine's build-artifact ignore section.
