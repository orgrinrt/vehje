**Date:** 2026-04-25
**Phase:** DOC
**Scope:** Source-only round. No doc-template changes. Round exists to open the doc-CL gate so the SRC CL can be authored and the directory rename executed.

# Round goal

Rename the 11 mockspace crate directories under `mock/crates/` from `clause-*` to `vehje-*` to match the crate identity rename committed in earlier rounds (`#216`, `202604300000`, `202604250637`). The crate `name` fields in each `Cargo.toml`, the workspace dependency aliases, and the per-crate template content already say `vehje-*`. Only the on-disk directory paths and the in-`mockspace.toml` lint scopes still hold the `clause-*` form.

# Doc changes

None. The crate-internal `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, and `README.md.tmpl` templates are already authored against the `vehje-*` identifier. No content updates are needed at the template level for this round.

# Comparison to deprecated changelist

Not deprecating an earlier CL. Backticked types touched at the doc-template level: none. The src CL handles everything.
