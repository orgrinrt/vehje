# Src changelist: complete the per-crate deepdive set

Doc-only round. The deepdives added in `202607240930_changelist.doc.lock.md` are documentation of the existing
design and implementation; they introduce no new public surface and require no source change. The
implementation already matches the DESIGNs each deepdive expands on: the four design-settled mechanisms landed
in round `202607240800` (the reachability binder rule, bounds-checked `GradeTable`, the value-arena typed
decode, and the lower cheap stratum), and the mechanisms the deepdives mark as deferred remain honestly
FIXME-marked where the design itself defers.

## No source changes

This round makes no `src/*.rs` change. The deepdives describe types and behaviour that already exist; the
`design-doc-source-mismatch` lint (every backticked type in a design doc must exist as a `pub` item) passed
over the new deepdives at commit, confirming the documentation matches the shipped surface.

Verification: `git diff` for this round touches only `*.md.tmpl` and the regenerated `docs/` tree, no
`crates/*/src/*.rs`.
