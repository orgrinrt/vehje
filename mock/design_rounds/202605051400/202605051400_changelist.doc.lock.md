**Date:** 2026-05-05
**Phase:** DOC
**Scope:** vehje (mock-root tmpls, every per-crate tmpl, mock/runtime-zig)
**Topic source files:**

- `202605051400_topic_doc_audit_2026_05_05.md`

# Round doc CL: documentation audit consolidation (vehje)

This CL applies the scope decisions from `202605051400_topic_doc_audit_2026_05_05.md` mechanically across every affected file. Findings are inherited from `mock/research/2026_05_05_doc_audit.md` (per-crate audit, ~75 findings) and `mock/research/2026_05_05_doc_audit_entrypoints.md` (entrypoint audit).

This is a doc-only CL.

## Files touched in this round

### Mock-root templates

| Path | Action |
|------|--------|
| `mock/DESIGN.md.tmpl` | Title flow fix per Decision 10 (`# vehje. Architecture Design` → `# Vehje: Architecture Design`); strip `stellar-heritage` references at lines 10, 13, 59-62 per Decision 6 (rewrite seed paragraph; drop or generic-frame the three "future extraction" bullets); strip `mock/design_rounds/` reference at line 15 per Decision 3; strip `.tmpl` self-references at lines 66-68 per Decision 3; resolve `clause` reference at line 13 per Decision 8 (the path may legitimately stay; reframe to disambiguate context) |
| `mock/PRINCIPLES.md.tmpl` | `saalis` reference replaced per Decision 9; round-shorthand vocabulary cleanup per Decision 3 (sample line 255 names "design rounds" in prose) |
| `mock/WORKFLOW.md.tmpl` | Verify rendering target per Decision 11; if Tier-1-rendered, rewrite consumer focus (lines 26, 28, 33 region naming `mockspace`, `cargo mock`, `.tmpl`); if Tier-2-only, no edit |
| `README.md` | No edit. Repo-root README is clean per the entrypoint audit. |

### Per-crate templates (11 crates plus runtime-zig)

For each crate, the doc CL touches the listed files per the per-crate audit's enumeration. The action column collapses multiple finding categories into the high-level scope.

| Crate | Files touched | Action |
|-------|--------------|--------|
| `vehje` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl`, `SHAME.md.tmpl` | `stellar-heritage` reference in BACKLOG (Decision 6); SHAME task-ID strip per Decision 5 result; `## Sources` block migrate per Decision 2; round-shorthand vocab cleanup; bullet-form remediation per Decision 4 |
| `vehje-codegen` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl`, `SHAME.md.tmpl` | `stellar-heritage` reference in BACKLOG (Decision 6); SHAME task-ID strip; `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation |
| `vehje-ir` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-lex` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-resolve` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-runtime-abi` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-runtime-driver` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-runtime-tests` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-schedule` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | **Decision 7 verification (load-bearing).** Read `crates/vehje-schedule/src/`; determine whether `PassDag` / `PassRegistry` / `Scheduler` / `Artifact` are: (a) missing-but-planned (doc stays, src CL adds), (b) renamed in source (doc updates to current names), or (c) replaced by different shapes (doc rewrites to match). Apply the verification result here. Plus `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-syntax` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `vehje-typecheck` | `DESIGN.md.tmpl`, `BACKLOG.md.tmpl`, `README.md.tmpl` | `## Sources` block migrate; round-shorthand vocab cleanup; bullet-form remediation per audit |
| `runtime-zig/*` | per-file per audit | `mock/runtime-zig/` audit findings applied per-file per the per-crate audit's enumeration |

### Tier 2 history files

Per Decision 2, every `## Sources` block migrates to a sibling Tier 2 history file:

| Path | Origin |
|------|--------|
| `mock/research/vehje-history.md` | `vehje/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-codegen-history.md` | `vehje-codegen/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-ir-history.md` | `vehje-ir/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-lex-history.md` | `vehje-lex/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-resolve-history.md` | `vehje-resolve/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-runtime-abi-history.md` | `vehje-runtime-abi/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-runtime-driver-history.md` | `vehje-runtime-driver/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-runtime-tests-history.md` | `vehje-runtime-tests/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-schedule-history.md` | `vehje-schedule/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-syntax-history.md` | `vehje-syntax/DESIGN.md.tmpl` `## Sources` |
| `mock/research/vehje-typecheck-history.md` | `vehje-typecheck/DESIGN.md.tmpl` `## Sources` |

## Lock criteria

The doc CL is ready to lock when every row in the per-crate table has been applied, every Tier 2 history file has been created, the workspace tag-cloud reports zero hits on banned vocabulary across `mock/crates/**/*.md.tmpl` and `mock/{DESIGN,PRINCIPLES,WORKFLOW}.md.tmpl`, the Tier 1 leakage grep reports zero hits on the same set, the SHAME-rendering verification (Decision 5) is recorded, the vehje-schedule doc-vs-source verification (Decision 7) is recorded, and `cargo mock` regenerates `docs/*.md` cleanly.

## Verification per file

The mockspace `design-doc-source-mismatch` lint covers backticked-type-name verification (Decision 7's most material part). Narrative-drift verification (Decisions 3, 4) is manual: lock-time review walks the audit research file's per-crate findings list. Decision 7's outcome (which side is canonical for vehje-schedule) is recorded in the lock comment.

## Following src CL (mentioned, not written)

After this doc CL is locked, a separate src CL covers:

1. Rustdoc on every `pub` item in `crates/**/src/**.rs` audited against the updated DESIGN.md.tmpl wording.
2. **vehje-schedule source addition (per Decision 7 outcome).** If the doc-side resolution is "doc is correct, source needs to add," the src CL ships `PassDag` / `PassRegistry` / `Scheduler` / `Artifact` per the DESIGN.
3. Final em-dash sweep across all `.rs` files in `crates/**/src/**.rs` and the `mock/runtime-zig/` source tree.

The src CL references this topic file by id.
