# Source changelist: wire run end to end

**Round:** 202607250259
**Phase:** IMPL
**Scope:** vehje (run + a test).

## CHANGE: `run` drives resolve -> check -> mint Checked -> emit
File: `mock/crates/vehje/src/lib.rs`
Replace the resolve-only stub with `run<T, Families, Effects, S>(target, arena, root, res: &mut Resolution, grades: &mut GradeTable, sink: &mut S) -> Outcome<(), Diagnostic>` where `T: Target`, `T::Supports: ContainsAll<Families>`, `T::Permits: ContainsAll<Effects>`, `S: ByteEmitter`. It resolves (a `ResolveError::Unresolved` becomes a `Phase::Resolve` diagnostic), runs `check` (a failure becomes a `Phase::Check` diagnostic), mints `check_for::<T, Families, Effects>(graded)`, and emits (a failure becomes a `Phase::Emit` diagnostic). Lower(ANF) integration and the runtime hand-off stay FIXMEs.
Verification: `grep -q 'check_for::<T, Families, Effects>' mock/crates/vehje/src/lib.rs && cargo test -p vehje`.

## CHANGE: `run_emits_the_program` test CREATE
File: `mock/crates/vehje/src/lib.rs`
A program built into an arena, passed through `run` with a `DebugTarget`, a caller-lent resolution and grade region, and a byte sink, emits the pre-order tag dump (asserts the sink bytes), proving the public entry runs end to end.
Verification: `cargo test -p vehje`.
